export default function PhotoCapture({ photos, onChange }) {
  return (
    <div className="bg-gray-800 text-gray-400 p-4 rounded-xl text-sm">
      Photo capture module (placeholder)
    </div>
  );
}